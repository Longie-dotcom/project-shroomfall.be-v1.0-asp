$kind: http-example
request:
  url: "{{baseUrl}}/api/Design/update"
  method: POST
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "key": "string",
        "version": 8423,
        "description": "string"
      }
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 1000
