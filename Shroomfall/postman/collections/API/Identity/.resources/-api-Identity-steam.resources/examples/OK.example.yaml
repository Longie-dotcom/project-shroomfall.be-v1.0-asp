$kind: http-example
request:
  url: "{{baseUrl}}/api/Identity/steam"
  method: POST
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "steamTicket": "string",
        "steamName": "string",
        "preferredLocale": "string"
      }
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 1000
