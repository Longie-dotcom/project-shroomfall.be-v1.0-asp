$kind: http-example
request:
  url: "{{baseUrl}}/api/Identity/register"
  method: POST
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "email": "string",
        "password": "string",
        "name": "string",
        "preferredLocale": "string"
      }
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 1000
